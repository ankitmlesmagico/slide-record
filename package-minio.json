{
  "name": "google-slides-recording-api-minio",
  "version": "2.1.0",
  "description": "Production-ready API for automated Google Slides presentation recording with MinIO storage",
  "main": "slides-recording-api-minio.js",
  "scripts": {
    "start": "node slides-recording-api-minio.js",
    "dev": "nodemon slides-recording-api-minio.js",
    "test": "node test-api-minio.js",
    "install-deps": "sudo apt update && sudo apt install -y xvfb google-chrome-stable ffmpeg nodejs npm fluxbox",
    "check-deps": "node -e \"console.log('Checking dependencies...'); const deps=['xvfb-run','google-chrome','ffmpeg','node']; deps.forEach(d => { try { require('child_process').execSync('which '+d, {stdio:'ignore'}); console.log('✓ '+d); } catch { console.log('✗ '+d+' (missing)'); } });\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.5.2",
    "uuid": "^9.0.1",
    "minio": "^8.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "axios": "^1.6.0"
  },
  "keywords": [
    "google-slides",
    "presentation",
    "recording",
    "automation",
    "puppeteer",
    "api",
    "video",
    "minio",
    "s3"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "os": [
    "linux"
  ]
}